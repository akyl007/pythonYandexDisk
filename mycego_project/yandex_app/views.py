from django.shortcuts import render, redirect
from django.http import HttpResponse
import requests


def index(request) -> HttpResponse:

    return

def extract_public_key(public_key: str) -> str:

    return

def files(request) -> HttpResponse:

    return

